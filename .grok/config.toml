# Project defaults for autonomous local development.
# Changes apply on the next session start (or re-enable via /always-approve).

[ui]
permission_mode = "always-approve"
yolo = true

[permission]
# Prefer deny-lists for hard stops rather than interactive asks.
deny = [
  "Bash(rm -rf /)",
  "Bash(rm -rf /*)",
]
